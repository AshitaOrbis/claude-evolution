{
  "title": "Hybrid Codebase Context Compressor — Model2Vec + sqlite-vec (98% token reduction)",
  "source": "https://news.ycombinator.com/item?id=47193064",
  "type": "mcp_server",
  "description": "MCP server that compresses large codebase context for Claude Code by combining Model2Vec sentence embeddings with sqlite-vec vector search. Claims 98% token reduction on large codebases. Unlike code-review-graph (knowledge graph approach, NEEDS_RESEARCH in pipeline), this uses dense vector embeddings to rank files by semantic relevance to the current query, then serves only the top-k most relevant files. Zero external API dependency — Model2Vec runs locally, sqlite-vec is a SQLite extension.",
  "discovered_at": "2026-03-26T00:00:00Z",
  "keywords": ["model2vec", "sqlite-vec", "vector-search", "codebase-compression", "token-reduction", "hybrid-retrieval", "embedding", "mcp", "context-management", "semantic-search"],
  "notes": "Comparison required with code-review-graph (49x token reduction, NEEDS_RESEARCH from 2026-03-19) before full evaluation. Both target the same problem (large codebase context overflow) but via different mechanisms: knowledge graph vs vector embeddings. Key questions: (1) GitHub repo URL and star count, (2) overlap with code-review-graph — evaluate together or separately, (3) local-only is a strong advantage vs cloud-dependent alternatives. If verified, likely scores 65-75 depending on community validation.",
  "evaluation": {
    "scores": {
      "integration_complexity": 70,
      "token_efficiency": 60,
      "capability_expansion": 80,
      "maintenance_burden": 70,
      "community_validation": 50
    },
    "total": 67.0,
    "decision": "NEEDS_RESEARCH",
    "reasoning": "Capability expansion is strong (80): addresses large codebase context overflow — we have mgrep for semantic search but not an MCP that auto-selects top-k relevant files for Claude's context window. Local-only operation (no external API) is a genuine advantage vs cloud-dependent alternatives. However, three critical unknowns prevent approval: (1) GitHub repo URL and star count are unverified — if <100 stars, community validation stays at 50 and total stays under 70; (2) the 98% token reduction claim is unverified — if accurate it would raise token efficiency score significantly; (3) relationship to code-review-graph (currently NEEDS_RESEARCH at 65.75) needs assessment before investing integration effort into a potentially redundant mechanism. Token efficiency scored conservatively at 60 pending claim verification. Integration at 70 (needs Model2Vec + sqlite-vec setup but both are local). Maintenance at 70 (local-only removes cloud dependency risk).",
    "evaluated_at": "2026-03-26T00:00:00Z"
  }
}
